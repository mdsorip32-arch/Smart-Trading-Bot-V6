import telebot
import yfinance as yf
import pandas_ta as ta
import time
import os

# ১. টোকেন ও আইডি কানেকশন
TOKEN = os.getenv('TOKEN')
USER_ID = os.getenv('USER_ID')
bot = telebot.TeleBot(TOKEN)

# ২. মার্কেট সিলেক্ট (BTC-USD)
SYMBOL = 'BTC-USD' 

def binary_style_signal():
    while True:
        try:
            # ৩. ডাটা ডাউনলোড করার আধুনিক পদ্ধতি (এরর এড়াতে)
            ticker = yf.Ticker(SYMBOL)
            df = ticker.history(period="1d", interval="1m")
            
            if not df.empty and len(df) > 10:
                # ৪. ক্যান্ডেল ও RSI এনালাইসিস
                df['RSI'] = ta.rsi(df['Close'], length=14)
                last_row = df.iloc[-1]
                
                # ৫. আপনার ১-মিনিটের ট্রেডিং লজিক
                # ক্যান্ডেল সবুজ হলে বা RSI কম থাকলে UP
                if last_row['Close'] > last_row['Open'] or last_row['RSI'] < 50:
                    direction = "🟢 **PREDICTION: UP** 🟢\n🚀 পরের ১ মিনিট উপরে থাকবে!"
                else:
                    direction = "🔴 **PREDICTION: DOWN** 🔴\n📉 পরের ১ মিনিট নিচে নামবে!"

                # ৬. সরাসরি সিগন্যাল মেসেজ
                msg = (
                    f"⏰ **1 MINUTE TRADE SIGNAL**\n"
                    f"💹 Asset: {SYMBOL}\n\n"
                    f"{direction}\n\n"
                    f"💵 এখনই ১ ডলারের ট্রেড নিন!"
                )
                bot.send_message(USER_ID, msg, parse_mode='Markdown')
            
            # ৭. ঠিক ৬০ সেকেন্ড পর পর নতুন ক্যান্ডেল চেক
            time.sleep(60) 
            
        except Exception as e:
            # যদি কোনো কারণে এরর হয়, ১০ সেকেন্ড পর আবার চেষ্টা করবে
            print(f"Error: {e}")
            time.sleep(10)

if __name__ == "__main__":
    bot.send_message(USER_ID, "🚀 আপনার ১-মিনিট প্রফিট বট রিস্টার্ট হয়েছে!\nএখন থেকে প্রতি মিনিটে ফ্রেশ সিগন্যাল পাবেন।")
    binary_style_signal()
